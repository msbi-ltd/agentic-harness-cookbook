[project]
name = "agentic-harness-cookbook"
version = "0.0.0"
description = "Reference implementations for the agentic-harness cookbook patterns."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
